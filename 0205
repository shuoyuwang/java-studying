FOR-EACH循环
for（<类型><变量>:<数组>）{}
适合遍历数组
1.没有分号
2.不知道是数组中哪一个位置
3.不能用来给数组赋值（不能修改数组）

循环数组中的每一个数，拿出来做每一轮的K
for ( int k : data ) 
{ 
   if ( x == k ) 
  { 
  found = true; break; 
  }
} 

比较

for ( int i=0; i<data.length; i++ ) 
{ 
  if ( x == data[i] ) 
  { 
  loc = i; break; 
  } 
}

计算素数只用到n的平方根即可

Math函数
sqrt：平方根
for ( int i=3; i<Math.sqrt(x); i+=2 )
{ 
if ( x % i == 0 )
  { 
  isPrime = false; break; 
  } 
}
